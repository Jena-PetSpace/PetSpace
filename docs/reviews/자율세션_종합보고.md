# 자율세션 종합보고 — 피드 P0 PATCH v1 + AppTheme v2

- 세션일: 2026-07-09~10 (사용자 부재 자율 세션) · 브랜치: `win-android-release`
- 프로토콜 준수: 구간별 커밋 9건 + 본 보고 커밋, **push·배포 미수행**, flutter analyze 0
- 최종 검증: `flutter analyze` **No issues found** / `flutter test` **356 통과·14 실패**
  - 14 실패는 전량 인계 베이스라인(emotion bloc 1 · health usecases 4 · feed bloc 9)과 동일 — **신규 실패 0**

## 구간별 커밋

| 구간 | 커밋 | 내용 |
|------|------|------|
| 1단계 A | `b756c09` | 분석 수치 UI 노출 제거 + 자동 게시 판정 |
| 1단계 B | `4a691fd` | 헤더 2층 재압축 + 그리드 뷰 제거 + FAB 앱바 이동 |
| 1단계 C | `e634b0d` | 뱃지 코드 통일 + 상대시간 + 카드 디테일 정리 |
| 2단계 T1 | `fc83efb` | AppTheme v2 토큰 재정의 |
| 2단계 T2 | `efc7976` | 하드코딩 컬러 토큰 치환 + 원빨강 전면 금지 |
| 2단계 T3 | `168bf42` | 공용 컴포넌트 v2 통일 |
| 2단계 T4 | `0ca5b35` | 이모지 UI 전역 제거 |
| 2단계 T5 | `f607745` | 내비게이션 v2 검수 |
| 추가 결정 반영 | `0de88e7` | 중앙 FAB 배경 actionBase 단색 원 (2026-07-10 승인) |

## 2단계 사전 점검 (착수 전 기록)

- 하드코딩 `Color(0x…)`: 총 **187건** (app_theme.dart 제외 **131건**, 36개 파일)
- 기존 토큰: app_theme.dart `static const Color` **50개**
- 공용 칩(CategoryChip) 사용처: **2곳**(피드 라운지 필터·홈 카테고리 필터) + ChoiceChip/pill류 8파일
- 추가 발견: 원빨강(`Colors.red*`) 직접 사용 **~110건** (T2에서 전량 치환)

## A-1 판정 (핵심 확인 보고)

**판정: 자동 게시 아님 — 사용자 opt-in 구조.** 사전 승인 분기 2 적용(차단하지 않고 수치 노출만 제거).

근거:
1. 피드 글 생성 경로는 3곳뿐이며 전부 사용자 명시 작성이다.
   - `pjh/lib/features/social/presentation/pages/feed_page.dart:501` — CreatePostBottomSheet(감정분석 첨부는 사용자가 AI 히스토리에서 직접 선택: `create_post_bottom_sheet.dart:361-375`)
   - `pjh/lib/features/social/presentation/pages/create_post_page.dart:585` — 라우터가 emotionAnalysis를 전달하는 경로 자체가 없음(`app_router.dart` `/create-post`는 petId/petName/imageUrl 쿼리만 수용)
   - `pjh/lib/features/feed_hub/presentation/pages/create_community_post_page.dart:67` — 라운지 수동 작성
2. 캡처의 자동 문구("…결과를 공유합니다", "건강분석 결과 50점 (확인불가)")는 커밋 `10edaf3`(emotion_result_page 피드 공유 연동)에서 도입됐다가 `c52e0bb`(Sprint1-STEP4 죽은 코드 삭제)에서 **이미 제거된 레거시 경로**의 산출물로, 현재 HEAD에 해당 문자열·생성 로직이 존재하지 않는다(`git grep` 0건). 화면에 보인 글은 **DB에 잔존한 과거 데이터**다.
3. 조치: 기능 플래그 불필요(차단 대상 없음). A-2로 피드 렌더 계층 수치 제거 —
   post_card 퍼센트 도넛(EmotionChart) → 감정 라벨 뱃지만, post_detail `라벨 N%` → 라벨만,
   작성 화면 첨부 프리뷰 2곳 퍼센트 제거. 수치는 데이터 모델에 보존.
4. 기존 테스트 글 정리는 지시서대로 **사용자(황정훈)의 대시보드 삭제 몫**으로 남김 —
   렌더는 고쳤어도 레거시 캡션 텍스트("불편함 30%" 등)는 DB `caption`에 그대로 남아 있음.

## 지시서와 다르게 판단한 지점 (전량 기록)

1. **A-1 분기**: "자동 생성 확정 시 비활성화"가 아닌 opt-in 확정 → 분기 2(수치 제거)만 수행. 레거시 경로가 이미 삭제돼 있어 "자동 캡션 로직의 수치·(확인불가) 문구 제거"는 대상 부재.
2. **C-3 자동 해시태그**: 캡처의 3종 태그(#반려동물감정분석 등)는 위 레거시 경로 소산으로 현재 코드에 없음. 현존 자동 부착은 create_post_page의 감정 기반 추천·펫이름 태그(`_suggestHashtags`)였고 이를 제거(지시서 취지 확대 적용). 외부 SNS 공유 텍스트의 해시태그는 피드가 아니므로 존치.
3. **C-4 파란 점**: 카테고리 컬러 잉여도, 읽음 표시도 아닌 **결함** — 미분류 글의 빈 categoryLabel이 빈 칩 컨테이너로 렌더된 것. 라벨 없으면 칩 숨김으로 수정.
4. **B-1 탭 인디케이터**: 별도 Divider 위젯과 TabBar 인디케이터가 다른 레이어에 그려지던 것을 TabBar 자체 dividerColor/dividerHeight로 통합 + Tab height 46→40 압축으로 해석·수정.
5. **T1 divider/border 분리**: 기존 단일 `dividerColor`가 보더·구분선을 겸하고 있어, `dividerColor`=#F1F3F6(구분선) + 신규 `border`=#E8EBEF(보더)로 분리 매핑. 보더로 쓰이던 주요 지점(카드·입력·칩·하단바)은 border 토큰으로 이관.
6. **T1 highlight 용도 충돌**: v2 컬러 표는 "CTA·뱃지 금지", T5는 "뱃지 highlight"로 상충 → 알림 카운트 뱃지(주의 시맨틱)는 highlight 허용, 정보성/장식 뱃지·CTA만 금지로 해석(P0 C-1과 정합).
7. **T2 로컬 디자인시스템 존치**: 아래 [v2-review 8파일] 참조 — 전역 토큰 치환 시 화면이 크게 깨져(중지 조건 4항, 보수적 판단) 값 존치 + `// v2-review` 헤더 표기.
8. **T2 브랜드 고정색**: 카카오 옐로(#FEE500)·구글 블루(#4285F4)는 브랜드 규정색으로 치환 제외(+주석).
9. **T2 원빨강 확대 적용**: 지시서 grep 대상은 `Color(0x`였으나 "원빨강 전면 금지" 원칙에 따라 `Colors.red*` ~110건도 errorColor(=highlight)로 일괄 치환. red[50] 배경→tilePastelRose, red[200] 보더→errorColor 35%.
10. **T3 버튼 치환 범위**: styleFrom/FAB 컨텍스트의 primaryColor 채움 30건만 actionBase로 전환. 비버튼 20건(아바타 배경·프리뷰 칩·스낵바 배경 등)은 도면적 채움 아님·의미 상이로 존치.
11. **T4 이모지 존치 예외**: 외부 공유 카드·공유 텍스트(share_helper류), 푸시 알림 본문, 북마크 컬렉션 이모지(사용자 데이터), 종 구분 이모지(quiz·MBTI 궁합의 🐶/🐱 — 아이콘 대체 불가, 일러스트 트랙), fortune_stars. 감정 이모지는 `AppTheme.getEmotionIcon`(임시 아이콘) 신설로 10개 화면 전환.
12. **테스트 삭제 1건**: 그리드 뷰 기능 제거(B-1)에 따라 feed_grid_filter.dart + 전용 테스트 삭제(케이스 4개 감소 — git 이력 보존).

## v2-review 로컬 팔레트 8파일 (치환 보류 — 실기기 확인 대상)

1. `pjh/lib/features/emotion/presentation/theme/emotion_result_tokens.dart` — 결과 페이지 베이지 리디자인 토큰("리포트 톤 별도 기획" 명시 대상)
2. `pjh/lib/features/emotion/presentation/widgets/ai_analysis_loading_widget.dart` — 분석 로딩 일러스트 팔레트
3. `pjh/lib/features/home/presentation/widgets/pet_passport_card.dart` — 펫 여권 카드 전용 팔레트
4. `pjh/lib/features/emotion/presentation/widgets/result/part_analysis_card.dart` — 결과 페이지 로컬 팔레트
5. `pjh/lib/features/emotion/presentation/widgets/result/emotion_summary_card.dart` — 결과 페이지 로컬 팔레트
6. `pjh/lib/features/emotion/presentation/widgets/result/fullscreen_photo_viewer.dart` — 사진 뷰어 근흑색 배경(기능색)
7. `pjh/lib/features/emotion/presentation/widgets/analysis_input/analysis_guide_sheet.dart` — 촬영 가이드 장식 그라데이션
8. `pjh/lib/features/emotion/presentation/widgets/emotion_loading_widget.dart` — 로딩 장식 그라데이션

> 실기기에서 어색한 화면은 캡처 공유 → 개별 판정 예정.

## [보류] 항목 (최종)

| 항목 | 상태 | 사유 |
|------|------|------|
| ~~중앙 도크(발바닥 FAB) 배경~~ | **해제 — `0de88e7` 반영** | 2026-07-10 결정: 배경은 코드 레이어 actionBase 단색 원, 글리프는 흰 발바닥만(에셋에 배경색 굽지 않음). `icon_fab_bg.svg`는 참조 0건으로 남김 |
| 감정 결과 페이지 로컬 팔레트 치환 | 보류 | "분석 결과 리포트 톤 별도 기획" 명시 + 치환 시 화면 파손(중지 조건 4항) |
| 일러스트성 표면(로딩·가이드 그라데이션, 여권 카드) | 보류 | 일러스트 자산 트랙(별도)과 병행 재검토 |
| 감정 일러스트 8종·빈 상태 일러스트 | 보류 | 자산 결정 대기 — 임시로 Material 아이콘 체계 적용 |
| 다크모드 v2 정합 | 보류(범위 외) | 단, deprecated alias(accentColor→actionBase) 값 변경이 darkTheme TextButton 전경색에 파급(코드 경로 무변경) — 다크 트랙에서 확인 요망 |

## 사용자 검증 가이드 (실기기)

1. **P0**: 감정·건강 분석 완료 후 피드에 자동 글 미생성 / 피드·상세의 감정 임베드에 수치 없음(라벨만) / 피드 헤더 2층 + 앱바 글쓰기 아이콘(검색 왼쪽) / 하단 탭 뱃지 코랄 / 라운지 미분류 글에 파란 점 없음 / 시간 표기 상대시간(7일 초과 시 YYYY년 M/DD)
2. **v2**: 버튼·링크 스틸블루(actionBase) / 에러·삭제가 코랄(원빨강 없음) / 빈 상태·뱃지·퀘스트가 아이콘(이모지 없음) / 홈 인기 주제 태그 단일 톤 / 중앙 FAB actionBase 원 + 흰 발바닥. **전역 치환이므로 피드 밖 화면 전부 훑기(특히 원빨강 치환 화면들)**
3. 테스트 글 정리: 레거시 자동 문구 글은 대시보드에서 직접 삭제 필요
4. push는 실기기 검증 후 사용자 확인 하에 진행

> 본 세션 산출물은 전부 로컬 커밋 상태, push·배포 미수행.

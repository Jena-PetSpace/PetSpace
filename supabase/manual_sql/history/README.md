# Historical manual SQL

These files document patches that were previously designed for manual
execution in Supabase SQL Editor. They are retained for audit, regression
tests, and canonical-schema reconciliation.

Do not execute this folder as a batch and do not treat it as the current
release plan. Use `../../releases/` for the reviewed production apply file.
